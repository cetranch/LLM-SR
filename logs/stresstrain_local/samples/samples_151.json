{"sample_order": 151, "function": "def equation(strain: np.ndarray, temp: np.ndarray, params: np.ndarray) -> np.ndarray:\n    \"\"\" Mathematical function for stress in Aluminium rod\n\n    Args:\n        strain: A numpy array representing observations of strain.\n        temp: A numpy array representing observations of temperature.\n        params: Array of numeric constants or parameters to be optimized\n\n    Return:\n        A numpy array representing stress as the result of applying the mathematical function to the inputs.\n    \"\"\"\n    \"\"\"\n    1. Elastic region: Linear relationship between stress and strain\n    2. Plastic region: Yield stress plus a non-linear strain hardening term\n    \"\"\"\n    # Elastic region\n    elastic_stress = params[0] * strain\n    \n    # Plastic region\n    # Yield stress (intercept)\n    yield_stress = params[1]\n    \n    # Strain hardening term\n    strain_hardening = np.power(strain, params[2]) if np.any(strain > 0) else 0\n    \n    # Final combined stress\n    stress = np.where(strain >= 0, elastic_stress + yield_stress + strain_hardening, elastic_stress + yield_stress)\n\n", "score": null}