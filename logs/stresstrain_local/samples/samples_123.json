{"sample_order": 123, "function": "def equation(strain: np.ndarray, temp: np.ndarray, params: np.ndarray) -> np.ndarray:\n    \"\"\" Mathematical function for stress in Aluminium rod\n\n    Args:\n        strain: A numpy array representing observations of strain.\n        temp: A numpy array representing observations of temperature.\n        params: Array of numeric constants or parameters to be optimized\n\n    Return:\n        A numpy array representing stress as the result of applying the mathematical function to the inputs.\n    \"\"\"\n    \"\"\"\n    Advanced version of `equation_v2`\n    \n    Args:\n        strain: A numpy array representing observations of strain.\n        temp: A numpy array representing observations of temperature.\n        params: Array of numeric constants or parameters to be optimized\n    \n    Return:\n        A numpy array representing stress as the result of applying the mathematical function to the inputs.\n    \"\"\"\n    \n    stress = np.zeros_like(strain)\n    index = np.where(strain > 0, 1, 0).astype(np.int32)\n    \n    elastic_region = np.where(index == 0, elastic_constant * strain, stress)\n    plastic_region = np.where(index == 1, plastic_strain(strain, temp, params), stress)\n    \n    stress = np.where(np.isnan(elastic_region), plastic_region, elastic_region)\n    \n    # Function to represent the plastic behavior\n    def plastic_strain(strain: np.ndarray, temp: np.ndarray, params: np.ndarray) -> np.ndarray:\n        y_0, m, b = params\n\n", "score": null}