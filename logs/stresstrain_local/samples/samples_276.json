{"sample_order": 276, "function": "def equation(strain: np.ndarray, temp: np.ndarray, params: np.ndarray) -> np.ndarray:\n    \"\"\" Mathematical function for stress in Aluminium rod\n\n    Args:\n        strain: A numpy array representing observations of strain.\n        temp: A numpy array representing observations of temperature.\n        params: Array of numeric constants or parameters to be optimized\n\n    Return:\n        A numpy array representing stress as the result of applying the mathematical function to the inputs.\n    \"\"\"\n    \"\"\"Improved version of `equation_v1`.\"\"\"\n    \n    # Parameters extraction\n    a = params[0]  # Elastic modulus\n    b = params[1:5] # Coefficients for the temperature effect\n    e = params[5]  # Plastic strain\n    \n    # Calculate stress based on the equation structure\n    stress = np.where(strain <= 1, a * strain, a * strain * np.ones_like(strain))\n    stress += np.exp(np.sum(b * np.log(temp / 300 + 1), axis=-1).reshape(-1, 1))\n    \n    # Plastic region\n    stress *= (1 - np.minimum(e * strain, 1.0))\n    \n    return stress\n\n", "score": null}